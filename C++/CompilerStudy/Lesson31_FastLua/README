# Fast lua
------------------------------
# Features:
    + parser combinator
    + register-based vm
    + variable-length byte code
    + mark-and-sweep gc
    + object(like lua table)
    + trick for
# TODO list:
    + local index for global variable
    + repl-switch-threading/token-threading/direct-threading(computed goto)
    + stack caching(cache top n? see jamVM)
    + dll import
    + COM
# TODO list 2:
    + hiden class
    + inline caching
    + ast->jit(like v8's full-compiler, use inline-caching to deal with type specific code)
    + type feedback
    + type specification
